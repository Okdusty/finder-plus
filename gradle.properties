org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Release signing passwords live in ~/.gradle/gradle.properties, not here:
#   finderplus.store.password=...
#   finderplus.key.password=...
# The keystore itself is keys/finderplus-release.jks, which is gitignored.
